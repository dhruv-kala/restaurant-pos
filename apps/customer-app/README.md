# Customer App

Reserved for the future customer ordering and loyalty Flutter application.

This directory must remain independent from the POS application. Reusable domain
contracts and design-system packages should be published from `shared/` rather
than imported from another app's `lib/` directory.
