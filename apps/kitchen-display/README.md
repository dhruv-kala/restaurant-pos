# Kitchen Display

Reserved for the future kitchen display application.

The KDS will consume authenticated outlet/station events and must not depend on
POS presentation code. Shared event contracts belong in `shared/`.
