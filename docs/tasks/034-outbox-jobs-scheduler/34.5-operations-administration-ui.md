# Task 34.5 - Operations Administration UI

## Status

Complete.

## Objective

Add admin operational visibility for outbox events, background jobs, attempts,
schedules, and recovery actions.

## Scope

Implement:

* shared Dart models and typed API client
* Riverpod providers
* admin job dashboard
* job list and detail screens
* attempt history screen
* scheduler list and pause/resume actions
* dead-letter retry/cancel controls

## Deferred

* arbitrary workflow designer
* custom script execution
* mobile restaurant-app UI

## Completion Criteria

* Authorized admins can inspect tenant-scoped jobs.
* Platform operators can inspect platform jobs.
* Manual recovery actions call protected APIs.
* Flutter analyze passes for changed packages/apps.

## Implementation Notes

Completed on 2026-06-16.

Implemented shared Dart models, typed jobs API client methods, admin Riverpod
providers, and an authorized admin jobs/scheduler operations console with:

* operational health cards
* background job list and detail dialog with attempts
* manual retry and cancellation actions
* dead-letter retry and resolution actions
* scheduled job list with pause/resume controls
* retry policy visibility and upsert action
* outbox event visibility

Validation:

* `flutter pub get` from `apps/admin`: passed
* `flutter analyze` from `apps/admin`: passed
