# Database

PostgreSQL schema ownership, database conventions, row-level security policies,
seed strategy, and generated schema documentation belong here.

Executable migrations belong in `backend/migrations`.
