# ServeIQ UI Kit

Shared Flutter theme, navigation, inputs, cards, dialogs, and other reusable
presentation primitives.

Widgets in this package must not contain restaurant business rules or import an
application feature.
