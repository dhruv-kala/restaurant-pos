# Restaurant POS Core

Framework-independent errors, formatting, validation, and foundational utilities.

This package must not depend on Flutter, Firebase, HTTP clients, or application
features.

Import with:

```dart
import 'package:restaurant_pos_core/restaurant_pos_core.dart';
```
