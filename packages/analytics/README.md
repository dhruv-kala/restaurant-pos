# Restaurant POS Analytics

Reserved for typed, privacy-safe product and operational analytics events.

Raw customer PII, credentials, payment data, and tenant secrets must never enter
analytics payloads.

```dart
import 'package:restaurant_pos_analytics/restaurant_pos_analytics.dart';
```
